spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        highlight_sql: true

jwt:
  secret-key: 59iJOIIERmaEoLI/Z/kKQW4v5eWySRi5987ovXdHgWNoRr+3DwQzlMcDetQQeANVBxqIMtUB5b2cmi1JTzDJEQ==
  expiration-time: 60000 # 1분
  refresh-expiration-time: 120000 # 2분

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

oauth2:
  kakao:
    client-id: test-client-id
    redirect-uri: http://localhost:8080/api/v1/auth/callback

app:
  auth:
    allowed-origins:
      - http://localhost:3000
    success-path: /login/success
    failure-path: /login/failure
    state-secret: test-secret-key
    cookie-secure: false
    cookie-same-site: Lax
